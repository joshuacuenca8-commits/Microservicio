spring:
  datasource:
    url: jdbc:h2:mem:productos_dev
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true

logging:
  level:
    com.ms_products: DEBUG
    org.springframework: INFO