spring:
  datasource:
    url: jdbc:mysql://${DB_HOST}:3306/productos_prod
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root123

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

logging:
  level:
    com.ms_products: WARN
    org.springframework: ERROR